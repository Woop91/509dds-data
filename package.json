{
  "name": "509dds-data",
  "version": "0.1.0",
  "private": true,
  "description": "509dds data pipeline + reference data; npm tooling exists solely to host the security pre-commit hook.",
  "scripts": {
    "ingest:markitdown": "python scripts/markitdown_bundle.py",
    "prepare": "husky",
    "security:scan:semgrep": "node scripts/run_security_scan.mjs semgrep",
    "security:scan:gitleaks": "node scripts/run_security_scan.mjs gitleaks"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
